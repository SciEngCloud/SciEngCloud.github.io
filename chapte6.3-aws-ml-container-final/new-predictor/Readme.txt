docker build -t yourname/predictor-new .
will build this, BUT FIRST you must modify this to include the correct aws user keys or (better) configure  the Docker file and sript so that the key can be supplied as parameters.
