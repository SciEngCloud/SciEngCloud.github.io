docker build -t yourname/table-manage .
will build this, BUT FIRST you must modify this to include the correct aws user keys or (better) configuree
the Docker file and sript so that the key can be supplied as parameters.
